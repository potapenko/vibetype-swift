Use https://github.com/potapenko/spec-first-bootstrap as the spec-first
reference.

This is a new or mostly empty Swift project for a native macOS menu bar
dictation app.

Read this project's `AGENTS.md`, `docs/specs/README.md`, and
`docs/specs/backlog.md`. Also read `docs/openwhispr_swiftui_codex_tz.md` when
working on initial MVP behavior.

Do not start implementing product features yet.

First:

1. confirm the minimum product area being specified
2. update or create the relevant spec under `docs/specs/features/`
3. preserve unknowns that still require user confirmation
4. recommend which implementation work should wait until the spec is stable
